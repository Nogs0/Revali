import { StyleSheet } from "react-native"

export default StyleSheet.create({
    container: {
        position: 'absolute',
        bottom: 0,
        width: `100%`,
        alignItems: 'center',
        justifyContent: 'center',
        height: '10%'
    },
    label: {
        fontSize: 26,
        fontWeight: 'bold'
    }
})