import { StyleSheet } from "react-native"

export default StyleSheet.create({
    container: {
        height: '100%'
    },
    infoContainer: {
        marginBottom: '25%',
        marginHorizontal: '5%'
    }
})